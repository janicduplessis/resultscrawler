'use strict';

describe('rc.about module', function() {

  beforeEach(module('rc.about'));

  describe('about controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var aboutCtrl = $controller('AboutCtrl');
      expect(aboutCtrl).toBeDefined();
    }));

  });
});
